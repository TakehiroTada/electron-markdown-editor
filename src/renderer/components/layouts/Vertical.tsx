import {default as styled, StyledComponent} from "styled-components"

const Flex: StyledComponent<"div", any, any, never> = styled.div`
  display: flex;
  flex-direction: column;
`

export default Flex
